language: ruby
env: BUNDLE_GEMFILE="$(ruby -e 'print File.expand_path %{test/Gemfile}')"
install: bundle install
script: bundle exec cucumber -r test/features/ test/features/
rvm:
  - 1.9.3
  - 2.0.0
